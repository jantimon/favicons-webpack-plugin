export function run(faviconOptions: any, context: any, compilation: any): Promise<any>;
/**
 * faviconsPublicPath always wins over compilerPublicPath
 * If both are undefined fallback to '/'
 */
export function getPublicPath(faviconsPublicPath: any, compilerPublicPath: any): any;
